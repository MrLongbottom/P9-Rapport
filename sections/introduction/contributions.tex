%Use of evaluation metrics for both topic queries og document queries
%Use pagerank without edges given from the dataset, but instead using the topic model's topics as edges
\todo[inline]{Contributions}